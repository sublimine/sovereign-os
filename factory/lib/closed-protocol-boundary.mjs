// Shared exposure contract, not evidence that a particular control was executed.
// Keep this a dependency-free leaf: authoring and review have different imports.
export const CLOSED_PROTOCOL_CONTROL_BOUNDARY='REPLICA CONTROL BOUNDARY: The public controls array defines exactly the controls the replica must report in its single closed response. That response receives only {schema,replicationId,publicProtocol}; it has no registry access, prior private reviews or receipts, tools, global plan or future events. Every control must therefore be executable and decidable from the supplied public premises and the replica own calculation. Do not put protocol approval/freeze authentication, other actors context admission, the material judgment of this still-unwritten result, opening, comparison or final acceptance in that array. Those are separate controller/reviewer obligations: preserve their full order in scope, stopping and limitations, not as supposedly completed replica checks. A protocol requiring unobservable or future controls must be returned before dispatch under its executable-method gate. For an already recorded attempt, retain any such control and its uncertainty; never relabel it PASS, delete it or claim that describing a future procedure proves its execution.';
